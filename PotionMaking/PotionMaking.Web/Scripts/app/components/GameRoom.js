'use strict';

var React = require('react');

var gameRoom = React.createClass({
    render: function () {
        return (
            <div>
               Game room component
            </div>
        )
    }
})

module.exports = gameRoom;