'use strict';

var React = require('react');
var App = React.createClass({
    render: function () {
        return (
            <div>
               This is inner app
            </div>
        )
    }
})

module.exports = App;